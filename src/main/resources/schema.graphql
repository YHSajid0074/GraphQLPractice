type User {
    id: ID!
    firstName: String!
    lastName: String!
    username: String!
}

type Query {
    allUsers: [User]!
}

type Mutation {
    addUser(firstName: String!, lastName: String!, username: String!, password: String!): User
}


